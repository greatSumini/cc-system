# Project Conventions (filled in by the porting team)

> This file is a **slot**. So that general-purpose agents behave correctly on *your* codebase,
> the porting team fills the sections below with their project's rules.
> If left blank, agents use only the conventions observed during context-gather (less accurate).
>
> This slot is the mechanism that prevents the "doesn't fit my code / false-positive review" problem
> of general-purpose harnesses like OMC.

## Language / Stack
- Primary language / framework:
- Package manager / runtime:

## Code Style
- Naming:
- File / directory structure rules:
- Formatter / linter (command):

## Verification Commands (used by verifier / implementer)
- Type check:
- Lint:
- Unit tests:
- Integration / e2e tests:
- Build:

## Forbidden / Caveats (false-positive prevention)
- <intentional patterns that should NOT be flagged in review>
- <areas not to touch / generated files>

## Domain Glossary
- <project-specific terms / abbreviations agents must know>

## Architecture Notes
- <core design intent, layer boundaries, dependency direction>
